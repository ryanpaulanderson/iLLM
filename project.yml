name: LLMChat
options:
  developmentLanguage: en
  createIntermediateGroups: true
  deploymentTarget:
    iOS: "18.6"

settings:
  base:
    SWIFT_VERSION: 5.10
    IPHONEOS_DEPLOYMENT_TARGET: 18.6

targets:
  LLMChat:
    type: application
    platform: iOS
    sources:
      - path: LLMChat
        excludes:
          - Tests/**
    resources:
      - path: LLMChat/Resources
        optional: true
      - path: LLMChat/Resources/LaunchScreen.storyboard
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.LLMChat
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: LLMChat
        INFOPLIST_KEY_UILaunchStoryboardName: LaunchScreen
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: ["UIInterfaceOrientationPortrait"]
        INFOPLIST_KEY_LSRequiresIPhoneOS: YES
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 0.1.0
    scheme:
      testTargets:
        - LLMChatTests

  LLMChatTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - LLMChat/Tests
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.example.LLMChatTests
    dependencies:
      - target: LLMChat